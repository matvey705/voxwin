# VoxWin — базовые зависимости (CPU-режим работает сразу)
faster-whisper>=1.1.0
sounddevice>=0.4.6
numpy>=1.26
PySide6>=6.6
keyboard>=0.13.5
pywin32>=306
# Использует системное хранилище сертификатов Windows для всего HTTPS
# (httpx/requests/urllib) — спасает скачивание моделей за корпоративными
# прокси с TLS-перехватом. Внедряется при старте VoxWin.
truststore>=0.9
